# v2.4 バランス確認

v2.4 はGitHub Pages / PWA対応の更新で、戦闘/育成の数値設定は変更していません。



- 基礎能力レーダーは、その機体の7グループ平均を可視化します。
- 棒グラフは現在値を比較する表示専用で、ゲーム計算には影響しません。
- グラフ上限は能力値の上昇に合わせて自動拡張するため、100を超える特化機でも表示が潰れません。
- 全兵装適性グラフは12兵装の平均値を表示し、現在装備は強調表示します。

`npm run smoke` / `npm run render-smoke` / `npm run balance` / `npm run longrun` で既存ロジックへの影響を確認します。